---
import Layout from "../layouts/Layout.astro";
import {
    BookOpen,
    Code2,
    FolderTree,
    Github,
    Settings,
    UploadCloud,
    ShieldCheck,
    Cpu,
    Sparkles,
    Gem,
    ArrowRight,
} from "lucide-react";

const technologies = [
    {
        name: "Astro",
        description: "Garante que seu site carregue em menos de 1 segundo.",
        icon: Cpu,
    },
    {
        name: "Tailwind CSS",
        description: "Estilo moderno e personalizado com precisão.",
        icon: Gem,
    },
    {
        name: "Animações (Motion)",
        description: "Efeitos suaves que trazem vida e luxo ao site.",
        icon: Sparkles,
    },
];

const structures = [
    { path: "src/pages/", desc: "Páginas principais do site." },
    {
        path: "src/components/",
        desc: "Onde ficam as seções (Preços, Galeria, etc).",
    },
    { path: "src/assets/", desc: "Sua galeria de fotos e ícones." },
];
---

<Layout title="Manual Técnico | Ateliê Sissa Rossi">
    <div
        class="min-h-screen text-white font-light pb-20 overflow-x-hidden bg-transparent"
    >
        <!-- Hero Header -->
        <section class="pt-32 pb-20 px-6 text-center border-b border-white/5">
            <div class="max-w-4xl mx-auto space-y-6">
                <div
                    class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-primary-gold/10 border border-primary-gold/20 text-primary-gold text-xs font-semibold tracking-widest uppercase"
                >
                    <ShieldCheck size={14} />
                    Documentação de Entrega
                </div>
                <h1
                    class="text-5xl md:text-7xl font-serif text-white tracking-tight"
                >
                    Manual do <span class="text-primary-gold italic"
                        >Projeto</span
                    >
                </h1>
                <p
                    class="text-lg md:text-xl text-white/50 max-w-2xl mx-auto leading-relaxed"
                >
                    Este guia foi preparado para garantir que você tenha total
                    controle sobre o seu novo ativo digital.
                </p>
            </div>
        </section>

        <main class="max-w-5xl mx-auto px-6 mt-20 space-y-32">
            <!-- Visão Geral -->
            <section
                class="grid grid-cols-1 md:grid-cols-2 gap-16 items-center"
            >
                <div class="space-y-8">
                    <div class="space-y-4">
                        <h2
                            class="text-3xl text-white font-medium flex items-center gap-4"
                        >
                            <span class="w-1.5 h-8 bg-primary-gold rounded-full"
                            ></span>
                            Arquitetura Premium
                        </h2>
                        <p class="text-white/60 text-lg leading-relaxed">
                            Seu site utiliza tecnologias de ponta para oferecer
                            a melhor performance do mercado atual.
                        </p>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        {
                            [
                                { label: "Performance", value: "100%" },
                                { label: "SEO", value: "Otimizado" },
                                { label: "Visual", value: "Premium" },
                                { label: "Mobile", value: "Ready" },
                            ].map((stat) => (
                                <div class="p-4 rounded-xl bg-white/5 border border-white/10">
                                    <span class="block text-[10px] text-white/30 uppercase tracking-widest mb-1">
                                        {stat.label}
                                    </span>
                                    <span class="text-xl text-primary-gold font-medium">
                                        {stat.value}
                                    </span>
                                </div>
                            ))
                        }
                    </div>
                </div>

                <div
                    class="p-8 rounded-3xl bg-white/[0.02] border border-white/10 space-y-6 relative overflow-hidden group"
                >
                    <div
                        class="absolute -top-20 -right-20 w-40 h-40 bg-primary-gold/10 rounded-full blur-3xl group-hover:bg-primary-gold/20 transition-all"
                    >
                    </div>
                    <h3 class="text-xl text-white font-medium">
                        Controle de Versão
                    </h3>
                    <p class="text-white/50 leading-relaxed font-light">
                        Seu código está seguro no GitHub. É o seu "cofre
                        digital" onde cada mudança fica registrada.
                    </p>
                    <a
                        href="https://github.com/WalaceRamosdev/sissarossi.git"
                        target="_blank"
                        class="flex items-center justify-between p-5 rounded-2xl bg-white/5 border border-white/10 hover:border-primary-gold/30 hover:bg-white/10 transition-all duration-500 text-white/80 hover:text-white group/link"
                    >
                        <div class="flex items-center gap-5">
                            <div
                                class="w-12 h-12 rounded-xl bg-white/5 flex items-center justify-center group-hover/link:bg-primary-gold/10 transition-colors"
                            >
                                <Github size={24} />
                            </div>
                            <div class="text-sm">
                                <span class="block font-medium"
                                    >Repositório Oficial</span
                                >
                                <span class="text-xs text-white/30 font-mono"
                                    >sissarossi.git</span
                                >
                            </div>
                        </div>
                        <ArrowRight
                            size={18}
                            className="text-primary-gold transform group-hover/link:translate-x-1 transition-transform"
                        />
                    </a>
                </div>
            </section>

            <!-- Tecnologias -->
            <section class="space-y-12">
                <div class="text-center space-y-4">
                    <h2 class="text-4xl text-white font-serif tracking-tight">
                        Poder Tecnológico
                    </h2>
                    <p class="text-white/40">
                        O que faz o seu site ser diferente dos outros.
                    </p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    {
                        technologies.map((tech) => (
                            <div class="p-10 rounded-[32px] bg-white/[0.03] border border-white/10 hover:bg-white/[0.05] transition-all text-center space-y-6">
                                <div class="w-16 h-16 mx-auto rounded-2xl bg-primary-gold/10 flex items-center justify-center text-primary-gold">
                                    <tech.icon size={32} />
                                </div>
                                <div class="space-y-3">
                                    <h3 class="text-xl text-white font-medium">
                                        {tech.name}
                                    </h3>
                                    <p class="text-white/50 text-sm leading-relaxed">
                                        {tech.description}
                                    </p>
                                </div>
                            </div>
                        ))
                    }
                </div>
            </section>

            <!-- Autonomia e Manutenção -->
            <section class="grid grid-cols-1 md:grid-cols-2 gap-12">
                <div
                    class="space-y-8 p-10 rounded-[40px] bg-gradient-to-br from-white/[0.05] to-transparent border border-white/10"
                >
                    <div
                        class="w-12 h-12 rounded-xl bg-primary-gold/20 flex items-center justify-center text-primary-gold mb-6"
                    >
                        <Settings size={24} />
                    </div>
                    <h2 class="text-2xl text-white font-medium italic">
                        Manutenção Simplificada
                    </h2>
                    <p class="text-white/60 leading-relaxed font-light">
                        Dividimos o site em blocos chamados "Componentes". Se
                        você precisar mudar um preço ou um serviço, basta editar
                        o componente correspondente.
                    </p>
                    <div class="space-y-3">
                        {
                            structures.map((s) => (
                                <div class="flex items-center justify-between p-4 rounded-xl bg-black/40 border border-white/5">
                                    <span class="text-primary-gold font-mono text-xs tracking-wider">
                                        {s.path}
                                    </span>
                                    <span class="text-white/40 text-[10px] uppercase font-bold tracking-widest">
                                        {s.desc}
                                    </span>
                                </div>
                            ))
                        }
                    </div>
                </div>

                <div class="space-y-12 py-6">
                    <div class="space-y-6">
                        <div class="flex items-center gap-4 text-primary-gold">
                            <UploadCloud size={24} />
                            <h3 class="text-2xl font-medium tracking-tight">
                                Atualizações Instantâneas
                            </h3>
                        </div>
                        <p
                            class="text-white/50 text-lg leading-relaxed font-light"
                        >
                            Estamos usando a **Vercel**. Isso significa que
                            qualquer correção feita no código é publicada no seu
                            site em segundos, automaticamente.
                        </p>
                        <div
                            class="p-6 border-l-2 border-primary-gold/30 bg-primary-gold/5 rounded-r-2xl"
                        >
                            <p class="text-primary-gold/80 italic text-sm">
                                "O sistema está sempre online, seguro e pronto
                                para receber novidades."
                            </p>
                        </div>
                    </div>

                    <div class="space-y-4">
                        <div class="flex items-center gap-4 text-white">
                            <BookOpen size={24} />
                            <h3 class="text-xl font-medium">Autonomia Total</h3>
                        </div>
                        <p class="text-white/40 leading-relaxed">
                            Qualquer desenvolvedor com conhecimento básico em
                            Astro e React pode assumir o projeto no futuro,
                            garantindo que você nunca fique "presa" a um único
                            prestador de serviço.
                        </p>
                    </div>
                </div>
            </section>
        </main>

        <!-- Footer -->
        <footer class="mt-40 pt-20 border-t border-white/5 text-center px-6">
            <div class="max-w-xl mx-auto space-y-8">
                <p
                    class="text-white/20 text-xs tracking-[0.5em] uppercase font-bold"
                >
                    Alpha Code © 2026
                </p>
                <div class="relative inline-block pb-4">
                    <span class="text-primary-gold font-serif italic text-2xl"
                        >A excelência está nos detalhes.</span
                    >
                    <div
                        class="absolute bottom-0 left-0 w-full h-[1px] bg-gradient-to-r from-transparent via-primary-gold to-transparent opacity-40"
                    >
                    </div>
                </div>
            </div>
        </footer>
    </div>
</Layout>

<style>
    /* Font definitions if needed, or using project defaults */
    @import url("https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Inter:wght@300;400;500;600&display=swap");

    :root {
        --font-serif: "Playfair Display", serif;
        --font-sans: "Inter", sans-serif;
    }

    body {
        font-family: var(--font-sans);
    }

    .font-serif {
        font-family: var(--font-serif);
    }
</style>
